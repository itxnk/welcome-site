apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: welcome-ingress

spec:
  rules:
  - host: {{ .Values.ingress.host }}

    http:
      paths:
      - path: /
        pathType: Prefix

        backend:
          service:
            name: welcome

            port:
              number: 80
